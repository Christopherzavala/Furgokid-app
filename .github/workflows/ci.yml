name: DEPRECATED - use ci-cd.yml

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/ci-cd.yml instead."
